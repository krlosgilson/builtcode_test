<%= breadcrumbs({"Pacientes": patients_path, "Editar": nil}) %>

<h1>Editar Paciente #<%= @patient.id %></h1>

<%= render 'form', patient: @patient %>

<%= link_to 'Mostrar', @patient %> |
<%= link_to 'Voltar', patients_path %>
